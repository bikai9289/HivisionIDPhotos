# 将 your-domain.com 替换为你的真实域名，并将域名 A 记录指向服务器公网 IP
your-domain.com {
  encode zstd gzip
  header {
    Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
  }
  reverse_proxy app:8000
}

